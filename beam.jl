immutable BeamItem
    contents
    score :: Number
end    

immutable Beam
    items
    size :: Number
end

function peek(beam :: Beam)
    stuff
end

function top_score(beam :: Beam)

function insert(beam :: Beam, item, score)
    
    if score 
